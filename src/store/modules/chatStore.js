const state = {
	title: 'Chatlist'
};

const mutations = {

};

const actions = {

}

const getters = {
	getChatTitle: state => state.title
}

export default {
	state,
	mutations,
	actions,
	getters
}